#include "main.h"
/**
*main-Entry point
*
*Return: Always 0 (Success)
*/
int main(void)
{
  char st[] = "_putchar";
  for (int ch = 0; ch < 8; ch++)
      _putchar(st[ch]);
  _putchar('\n');
  return (0);
}
